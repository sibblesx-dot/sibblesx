[build]
  command = "echo 'No build step needed'"
  publish = "."

[dev]
  command = "python3 -m http.server 8000"
  port = 8000
  targetPort = 8000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
