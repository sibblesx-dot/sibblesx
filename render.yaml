services:
  - type: web
    name: sibblesX
    envVarGroups:
      - id: backend_env
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    staticPublishPath: .
